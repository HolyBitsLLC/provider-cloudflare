---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: applications.access.cloudflare.
spec:
  group: access.cloudflare.
  names:
    categories:
    - crossplane
    - managed
    - cloudflare
    kind: Application
    listKind: ApplicationList
    plural: applications
    singular: application
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Application is the Schema for the Applications API. Provides
          a Cloudflare Access Application resource. Access Applications are used to
          restrict access to a whole application using an authorisation gateway managed
          by Cloudflare.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ApplicationSpec defines the desired state of Application
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  accountId:
                    description: |-
                      (String) The account identifier to target for the resource. Conflicts with zone_id.
                      The account identifier to target for the resource. Conflicts with `zone_id`.
                    type: string
                  allowAuthenticateViaWarp:
                    description: |-
                      (Boolean) When set to true, users can authenticate to this application using their WARP session. When set to false this application will always require direct IdP authentication. This setting always overrides the organization setting for WARP authentication.
                      When set to true, users can authenticate to this application using their WARP session. When set to false this application will always require direct IdP authentication. This setting always overrides the organization setting for WARP authentication.
                    type: boolean
                  allowedIdps:
                    description: |-
                      (Set of String) The identity providers selected for the application.
                      The identity providers selected for the application.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  appLauncherLogoUrl:
                    description: |-
                      (String) The logo URL of the app launcher.
                      The logo URL of the app launcher.
                    type: string
                  appLauncherVisible:
                    description: |-
                      (Boolean) Option to show/hide applications in App Launcher. Defaults to true.
                      Option to show/hide applications in App Launcher. Defaults to `true`.
                    type: boolean
                  autoRedirectToIdentity:
                    description: |-
                      (Boolean) Option to skip identity provider selection if only one is configured in allowed_idps. Defaults to false.
                      Option to skip identity provider selection if only one is configured in `allowed_idps`. Defaults to `false`.
                    type: boolean
                  bgColor:
                    description: |-
                      (String) The background color of the app launcher.
                      The background color of the app launcher.
                    type: string
                  corsHeaders:
                    description: |-
                      (Block List) CORS configuration for the Access Application. See below for reference structure. (see below for nested schema)
                      CORS configuration for the Access Application. See below for reference structure.
                    items:
                      properties:
                        allowAllHeaders:
                          description: |-
                            (Boolean) Value to determine whether all HTTP headers are exposed.
                            Value to determine whether all HTTP headers are exposed.
                          type: boolean
                        allowAllMethods:
                          description: |-
                            (Boolean) Value to determine whether all methods are exposed.
                            Value to determine whether all methods are exposed.
                          type: boolean
                        allowAllOrigins:
                          description: |-
                            (Boolean) Value to determine whether all origins are permitted to make CORS requests.
                            Value to determine whether all origins are permitted to make CORS requests.
                          type: boolean
                        allowCredentials:
                          description: |-
                            (Boolean) Value to determine if credentials (cookies, authorization headers, or TLS client certificates) are included with requests.
                            Value to determine if credentials (cookies, authorization headers, or TLS client certificates) are included with requests.
                          type: boolean
                        allowedHeaders:
                          description: |-
                            (Set of String) List of HTTP headers to expose via CORS.
                            List of HTTP headers to expose via CORS.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        allowedMethods:
                          description: |-
                            (Set of String) List of methods to expose via CORS.
                            List of methods to expose via CORS.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        allowedOrigins:
                          description: |-
                            (Set of String) List of origins permitted to make CORS requests.
                            List of origins permitted to make CORS requests.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        maxAge:
                          description: |-
                            (Number) The maximum time a preflight request will be cached.
                            The maximum time a preflight request will be cached.
                          type: number
                      type: object
                    type: array
                  customDenyMessage:
                    description: |-
                      (String) Option that returns a custom error message when a user is denied access to the application.
                      Option that returns a custom error message when a user is denied access to the application.
                    type: string
                  customDenyUrl:
                    description: |-
                      (String) Option that redirects to a custom URL when a user is denied access to the application via identity based rules.
                      Option that redirects to a custom URL when a user is denied access to the application via identity based rules.
                    type: string
                  customNonIdentityDenyUrl:
                    description: |-
                      (String) Option that redirects to a custom URL when a user is denied access to the application via non identity rules.
                      Option that redirects to a custom URL when a user is denied access to the application via non identity rules.
                    type: string
                  customPages:
                    description: |-
                      (Set of String) The custom pages selected for the application.
                      The custom pages selected for the application.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  destinations:
                    description: |-
                      (Block List) A destination secured by Access. Only present for self_hosted, vnc, and ssh applications. Always includes the value set as domain. Supersedes self_hosted_domains to allow for more flexibility in defining different types of destinations. Conflicts with self_hosted_domains. (see below for nested schema)
                      A destination secured by Access. Only present for self_hosted, vnc, and ssh applications. Always includes the value set as `domain`. Supersedes `self_hosted_domains` to allow for more flexibility in defining different types of destinations. Conflicts with `self_hosted_domains`.
                    items:
                      properties:
                        cidr:
                          description: |-
                            (String) The private CIDR of the destination. Only valid when type=private. IPs are computed as /32 cidr. Private destinations are an early access feature and gated behind a feature flag.
                            The private CIDR of the destination. Only valid when type=private. IPs are computed as /32 cidr. Private destinations are an early access feature and gated behind a feature flag.
                          type: string
                        hostname:
                          description: |-
                            (String) The private hostname of the destination. Only valid when type=private. Private hostnames currently match only Server Name Indications (SNI). Private destinations are an early access feature and gated behind a feature flag.
                            The private hostname of the destination. Only valid when type=private. Private hostnames currently match only Server Name Indications (SNI). Private destinations are an early access feature and gated behind a feature flag.
                          type: string
                        l4Protocol:
                          description: |-
                            (String) The l4 protocol that matches this destination. Only valid when type=private. Private destinations are an early access feature and gated behind a feature flag.
                            The l4 protocol that matches this destination. Only valid when type=private. Private destinations are an early access feature and gated behind a feature flag.
                          type: string
                        portRange:
                          description: |-
                            (String) The port range of the destination. Only valid when type=private. Single ports are supported. Private destinations are an early access feature and gated behind a feature flag.
                            The port range of the destination. Only valid when type=private. Single ports are supported. Private destinations are an early access feature and gated behind a feature flag.
                          type: string
                        type:
                          description: |-
                            (String) The application type. Available values: app_launcher, bookmark, biso, dash_sso, saas, self_hosted, ssh, vnc, warp, infrastructure. Defaults to self_hosted.
                            The destination type. Available values: `public`, `private`. Defaults to `public`.
                          type: string
                        uri:
                          description: |-
                            (String) The public URI of the destination. Can include a domain and path with wildcards. Only valid when type=public.
                            The public URI of the destination. Can include a domain and path with wildcards. Only valid when type=public.
                          type: string
                        vnetId:
                          description: |-
                            (String) The VNet ID of the destination. Only valid when type=private. Private destinations are an early access feature and gated behind a feature flag.
                            The VNet ID of the destination. Only valid when type=private. Private destinations are an early access feature and gated behind a feature flag.
                          type: string
                      type: object
                    type: array
                  domain:
                    description: |-
                      (String) The primary hostname and path that Access will secure. If the app is visible in the App Launcher dashboard, this is the domain that will be displayed.
                      The primary hostname and path that Access will secure. If the app is visible in the App Launcher dashboard, this is the domain that will be displayed.
                    type: string
                  domainType:
                    description: |-
                      (String) The type of the primary domain. Available values: public, private.
                      The type of the primary domain. Available values: `public`, `private`.
                    type: string
                  enableBindingCookie:
                    description: |-
                      (Boolean) Option to provide increased security against compromised authorization tokens and CSRF attacks by requiring an additional "binding" cookie on requests. Defaults to false.
                      Option to provide increased security against compromised authorization tokens and CSRF attacks by requiring an additional "binding" cookie on requests. Defaults to `false`.
                    type: boolean
                  footerLinks:
                    description: |-
                      (Block Set) The footer links of the app launcher. (see below for nested schema)
                      The footer links of the app launcher.
                    items:
                      properties:
                        name:
                          description: |-
                            (String) Friendly name of the Access Application.
                            The name of the footer link.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the footer link.
                            The URL of the footer link.
                          type: string
                      type: object
                    type: array
                  headerBgColor:
                    description: |-
                      (String) The background color of the header bar in the app launcher.
                      The background color of the header bar in the app launcher.
                    type: string
                  httpOnlyCookieAttribute:
                    description: |-
                      (Boolean) Option to add the HttpOnly cookie flag to access tokens.
                      Option to add the `HttpOnly` cookie flag to access tokens.
                    type: boolean
                  landingPageDesign:
                    description: |-
                      (Block List, Max: 1) The landing page design of the app launcher. (see below for nested schema)
                      The landing page design of the app launcher.
                    items:
                      properties:
                        buttonColor:
                          description: |-
                            (String) The button color of the landing page.
                            The button color of the landing page.
                          type: string
                        buttonTextColor:
                          description: |-
                            (String) The button text color of the landing page.
                            The button text color of the landing page.
                          type: string
                        imageUrl:
                          description: |-
                            (String) The URL of the image to be displayed in the landing page.
                            The URL of the image to be displayed in the landing page.
                          type: string
                        message:
                          description: |-
                            (String) The message of the landing page.
                            The message of the landing page.
                          type: string
                        title:
                          description: |-
                            (String) The title of the landing page.
                            The title of the landing page.
                          type: string
                      type: object
                    type: array
                  logoUrl:
                    description: |-
                      (String) Image URL for the logo shown in the app launcher dashboard.
                      Image URL for the logo shown in the app launcher dashboard.
                    type: string
                  name:
                    description: |-
                      (String) Friendly name of the Access Application.
                      Friendly name of the Access Application.
                    type: string
                  optionsPreflightBypass:
                    description: |-
                      (Boolean) Allows options preflight requests to bypass Access authentication and go directly to the origin. Cannot turn on if cors_headers is set. Defaults to false.
                      Allows options preflight requests to bypass Access authentication and go directly to the origin. Cannot turn on if cors_headers is set. Defaults to `false`.
                    type: boolean
                  policies:
                    description: |-
                      (List of String) The policies associated with the application, in ascending order of precedence. Warning: Do not use this field while you still have this application ID referenced as application_id in any cloudflare_access_policy resource, as it can result in an inconsistent state.
                      The policies associated with the application, in ascending order of precedence. Warning: Do not use this field while you still have this application ID referenced as `application_id` in any `cloudflare_access_policy` resource, as it can result in an inconsistent state.
                    items:
                      type: string
                    type: array
                  saasApp:
                    description: |-
                      (Block List, Max: 1) SaaS configuration for the Access Application. (see below for nested schema)
                      SaaS configuration for the Access Application.
                    items:
                      properties:
                        accessTokenLifetime:
                          description: |-
                            (String) The lifetime of the Access Token after creation. Valid units are m and h. Must be greater than or equal to 1m and less than or equal to 24h.
                            The lifetime of the Access Token after creation. Valid units are `m` and `h`. Must be greater than or equal to 1m and less than or equal to 24h.
                          type: string
                        allowPkceWithoutClientSecret:
                          description: |-
                            (Boolean) Allow PKCE flow without a client secret.
                            Allow PKCE flow without a client secret.
                          type: boolean
                        appLauncherUrl:
                          description: |-
                            (String) The URL where this applications tile redirects users.
                            The URL where this applications tile redirects users.
                          type: string
                        authType:
                          description: |-
                            (String) Modifying this attribute will force creation of a new resource.
                            **Modifying this attribute will force creation of a new resource.**
                          type: string
                        consumerServiceUrl:
                          description: |-
                            (String) The service provider's endpoint that is responsible for receiving and parsing a SAML assertion.
                            The service provider's endpoint that is responsible for receiving and parsing a SAML assertion.
                          type: string
                        customAttribute:
                          description: |-
                            (Block List) Custom attribute mapped from IDPs. (see below for nested schema)
                            Custom attribute mapped from IDPs.
                          items:
                            properties:
                              friendlyName:
                                description: |-
                                  (String) A friendly name for the attribute as provided to the SaaS app.
                                  A friendly name for the attribute as provided to the SaaS app.
                                type: string
                              name:
                                description: |-
                                  (String) Friendly name of the Access Application.
                                  The name of the attribute as provided to the SaaS app.
                                type: string
                              nameFormat:
                                description: |-
                                  (String) A globally unique name for an identity or service provider.
                                  A globally unique name for an identity or service provider.
                                type: string
                              required:
                                description: |-
                                  (Boolean) True if the attribute must be always present.
                                  True if the attribute must be always present.
                                type: boolean
                              source:
                                description: '(Block List, Min: 1, Max: 1) (see below
                                  for nested schema)'
                                items:
                                  properties:
                                    name:
                                      description: |-
                                        (String) Friendly name of the Access Application.
                                        The name of the attribute as provided by the IDP.
                                      type: string
                                    nameByIdp:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) A mapping from IdP ID to claim name.
                                        A mapping from IdP ID to claim name.
                                      type: object
                                      x-kubernetes-map-type: granular
                                  type: object
                                type: array
                            type: object
                          type: array
                        customClaim:
                          description: |-
                            (Block List) Custom claim mapped from IDPs. (see below for nested schema)
                            Custom claim mapped from IDPs.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) Friendly name of the Access Application.
                                  The name of the attribute as provided to the SaaS app.
                                type: string
                              required:
                                description: |-
                                  (Boolean) True if the attribute must be always present.
                                  True if the attribute must be always present.
                                type: boolean
                              scope:
                                description: |-
                                  (String) The scope of the claim.
                                  The scope of the claim.
                                type: string
                              source:
                                description: '(Block List, Min: 1, Max: 1) (see below
                                  for nested schema)'
                                items:
                                  properties:
                                    name:
                                      description: |-
                                        (String) Friendly name of the Access Application.
                                        The name of the attribute as provided by the IDP.
                                      type: string
                                    nameByIdp:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) A mapping from IdP ID to claim name.
                                        A mapping from IdP ID to claim name.
                                      type: object
                                      x-kubernetes-map-type: granular
                                  type: object
                                type: array
                            type: object
                          type: array
                        defaultRelayState:
                          description: |-
                            (String) The relay state used if not provided by the identity provider.
                            The relay state used if not provided by the identity provider.
                          type: string
                        grantTypes:
                          description: |-
                            (Set of String) The OIDC flows supported by this application.
                            The OIDC flows supported by this application.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        groupFilterRegex:
                          description: |-
                            (String) A regex to filter Cloudflare groups returned in ID token and userinfo endpoint.
                            A regex to filter Cloudflare groups returned in ID token and userinfo endpoint.
                          type: string
                        hybridAndImplicitOptions:
                          description: |-
                            (Block List, Max: 1) Hybrid and Implicit Flow options. (see below for nested schema)
                            Hybrid and Implicit Flow options.
                          items:
                            properties:
                              returnAccessTokenFromAuthorizationEndpoint:
                                description: |-
                                  (Boolean) If true, the authorization endpoint will return an access token.
                                  If true, the authorization endpoint will return an access token.
                                type: boolean
                              returnIdTokenFromAuthorizationEndpoint:
                                description: |-
                                  (Boolean) If true, the authorization endpoint will return an id token.
                                  If true, the authorization endpoint will return an id token.
                                type: boolean
                            type: object
                          type: array
                        nameIdFormat:
                          description: |-
                            (String) The format of the name identifier sent to the SaaS application.
                            The format of the name identifier sent to the SaaS application.
                          type: string
                        nameIdTransformJsonata:
                          description: |-
                            (String) A JSONata expression that transforms an application's user identities into a NameID value for its SAML assertion. This expression should evaluate to a singular string. The output of this expression can override the name_id_format setting.
                            A [JSONata](https://jsonata.org/) expression that transforms an application's user identities into a NameID value for its SAML assertion. This expression should evaluate to a singular string. The output of this expression can override the `name_id_format` setting.
                          type: string
                        redirectUris:
                          description: |-
                            (Set of String) The permitted URL's for Cloudflare to return Authorization codes and Access/ID tokens.
                            The permitted URL's for Cloudflare to return Authorization codes and Access/ID tokens.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        refreshTokenOptions:
                          description: |-
                            (Block List) Refresh token grant options. (see below for nested schema)
                            Refresh token grant options.
                          items:
                            properties:
                              lifetime:
                                description: |-
                                  (String) How long a refresh token will be valid for after creation. Valid units are m, h and d. Must be longer than 1m.
                                  How long a refresh token will be valid for after creation. Valid units are `m`, `h` and `d`. Must be longer than 1m.
                                type: string
                            type: object
                          type: array
                        samlAttributeTransformJsonata:
                          description: |-
                            (String) A JSONata expression that transforms an application's user identities into attribute assertions in the SAML response. The expression can transform id, email, name, and groups values. It can also transform fields listed in the saml_attributes or oidc_fields of the identity provider used to authenticate. The output of this expression must be a JSON object.
                            A [JSONata](https://jsonata.org/) expression that transforms an application's user identities into attribute assertions in the SAML response. The expression can transform id, email, name, and groups values. It can also transform fields listed in the saml_attributes or oidc_fields of the identity provider used to authenticate. The output of this expression must be a JSON object.
                          type: string
                        scopes:
                          description: |-
                            (Set of String) Define the user information shared with access.
                            Define the user information shared with access.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        spEntityId:
                          description: |-
                            (String) A globally unique name for an identity or service provider.
                            A globally unique name for an identity or service provider.
                          type: string
                      type: object
                    type: array
                  sameSiteCookieAttribute:
                    description: |-
                      site cookie setting for access tokens. Available values: none, lax, strict.
                      Defines the same-site cookie setting for access tokens. Available values: `none`, `lax`, `strict`.
                    type: string
                  scimConfig:
                    description: |-
                      (Block List, Max: 1) Configuration for provisioning to this application via SCIM. This is currently in closed beta. (see below for nested schema)
                      Configuration for provisioning to this application via SCIM. This is currently in closed beta.
                    items:
                      properties:
                        authentication:
                          description: |-
                            (Block List) Attributes for configuring HTTP Basic, OAuth Bearer token, or OAuth 2 authentication schemes for SCIM provisioning to an application. (see below for nested schema)
                            Attributes for configuring HTTP Basic, OAuth Bearer token, or OAuth 2 authentication schemes for SCIM provisioning to an application.
                          items:
                            properties:
                              authorizationUrl:
                                description: |-
                                  (String) URL used to generate the auth code used during token generation.
                                  URL used to generate the auth code used during token generation.
                                type: string
                              clientId:
                                description: |-
                                  (String) The application client id.
                                  Client ID used to authenticate when generating a token for authenticating with the remote SCIM service.
                                type: string
                              clientSecret:
                                description: |-
                                  (String, Sensitive) The application client secret, only returned on initial apply.
                                  Secret used to authenticate when generating a token for authenticating with the remove SCIM service.
                                type: string
                              password:
                                description: (String)
                                type: string
                              scheme:
                                description: |-
                                  (String) The authentication scheme to use when making SCIM requests to this application.
                                  The authentication scheme to use when making SCIM requests to this application.
                                type: string
                              scopes:
                                description: |-
                                  (Set of String) Define the user information shared with access.
                                  The authorization scopes to request when generating the token used to authenticate with the remove SCIM service.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              token:
                                description: |-
                                  (String) Token used to authenticate with the remote SCIM service.
                                  Token used to authenticate with the remote SCIM service.
                                type: string
                              tokenUrl:
                                description: |-
                                  (String) URL used to generate the token used to authenticate with the remote SCIM service.
                                  URL used to generate the token used to authenticate with the remote SCIM service.
                                type: string
                              user:
                                description: |-
                                  (String) User name used to authenticate with the remote SCIM service.
                                  User name used to authenticate with the remote SCIM service.
                                type: string
                            type: object
                          type: array
                        deactivateOnDelete:
                          description: |-
                            (Boolean) If false, propagates DELETE requests to the target application for SCIM resources. If true, sets 'active' to false on the SCIM resource. Note: Some targets do not support DELETE operations.
                            If false, propagates DELETE requests to the target application for SCIM resources. If true, sets 'active' to false on the SCIM resource. Note: Some targets do not support DELETE operations.
                          type: boolean
                        enabled:
                          description: |-
                            (Boolean) Whether SCIM provisioning is turned on for this application.
                            Whether SCIM provisioning is turned on for this application.
                          type: boolean
                        idpUid:
                          description: |-
                            (String) The UIDs of the IdP to use as the source for SCIM resources to provision to this application.
                            The UIDs of the IdP to use as the source for SCIM resources to provision to this application.
                          type: string
                        mappings:
                          description: |-
                            (Block List) A list of mappings to apply to SCIM resources before provisioning them in this application. These can transform or filter the resources to be provisioned. (see below for nested schema)
                            A list of mappings to apply to SCIM resources before provisioning them in this application. These can transform or filter the resources to be provisioned.
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) Whether SCIM provisioning is turned on for this application.
                                  Whether or not this mapping is enabled.
                                type: boolean
                              filter:
                                description: |-
                                  (String) A SCIM filter expression that matches resources that should be provisioned to this application.
                                  A [SCIM filter expression](https://datatracker.ietf.org/doc/html/rfc7644#section-3.4.2.2) that matches resources that should be provisioned to this application.
                                type: string
                              operations:
                                description: |-
                                  (Block List, Max: 1) Whether or not this mapping applies to creates, updates, or deletes. (see below for nested schema)
                                  Whether or not this mapping applies to creates, updates, or deletes.
                                items:
                                  properties:
                                    create:
                                      description: |-
                                        (Boolean) Whether or not this mapping applies to create (POST) operations.
                                        Whether or not this mapping applies to create (POST) operations.
                                      type: boolean
                                    delete:
                                      description: |-
                                        (Boolean) Whether or not this mapping applies to DELETE operations.
                                        Whether or not this mapping applies to DELETE operations.
                                      type: boolean
                                    update:
                                      description: |-
                                        (Boolean) Whether or not this mapping applies to update (PATCH/PUT) operations.
                                        Whether or not this mapping applies to update (PATCH/PUT) operations.
                                      type: boolean
                                  type: object
                                type: array
                              schema:
                                description: |-
                                  (String) Which SCIM resource type this mapping applies to.
                                  Which SCIM resource type this mapping applies to.
                                type: string
                              strictness:
                                description: |-
                                  (String) How strictly to adhere to outbound resource schemas when provisioning to this mapping. "strict" will remove unknown values when provisioning, while "passthrough" will pass unknown values to the target.
                                  How strictly to adhere to outbound resource schemas when provisioning to this mapping. "strict" will remove unknown values when provisioning, while "passthrough" will pass unknown values to the target.
                                type: string
                              transformJsonata:
                                description: |-
                                  (String) A JSONata expression that transforms the resource before provisioning it in the application.
                                  A [JSONata](https://jsonata.org/) expression that transforms the resource before provisioning it in the application.
                                type: string
                            type: object
                          type: array
                        remoteUri:
                          description: |-
                            compatible API.
                            The base URI for the application's SCIM-compatible API.
                          type: string
                      type: object
                    type: array
                  selfHostedDomains:
                    description: |-
                      (Set of String, Deprecated) List of public domains secured by Access. Only present for self_hosted, vnc, and ssh applications. Always includes the value set as domain. Deprecated in favor of destinations and will be removed in the next major version. Conflicts with destinations.
                      List of public domains secured by Access. Only present for self_hosted, vnc, and ssh applications. Always includes the value set as `domain`. Deprecated in favor of `destinations` and will be removed in the next major version. Conflicts with `destinations`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  serviceAuth401Redirect:
                    description: |-
                      (Boolean) Option to return a 401 status code in service authentication rules on failed requests. Defaults to false.
                      Option to return a 401 status code in service authentication rules on failed requests. Defaults to `false`.
                    type: boolean
                  sessionDuration:
                    description: |-
                      authorise. Must be in the format 48h or 2h45m. Defaults to 24h.
                      How often a user will be forced to re-authorise. Must be in the format `48h` or `2h45m`. Defaults to `24h`.
                    type: string
                  skipAppLauncherLoginPage:
                    description: |-
                      (Boolean) Option to skip the App Launcher landing page. Defaults to false.
                      Option to skip the App Launcher landing page. Defaults to `false`.
                    type: boolean
                  skipInterstitial:
                    description: |-
                      (Boolean) Option to skip the authorization interstitial when using the CLI. Defaults to false.
                      Option to skip the authorization interstitial when using the CLI. Defaults to `false`.
                    type: boolean
                  tags:
                    description: |-
                      (Set of String) The itags associated with the application.
                      The itags associated with the application.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  targetCriteria:
                    description: |-
                      (Block List) The payload for an infrastructure application which defines the port, protocol, and target attributes. Only applicable to Infrastructure Applications, in which case this field is required. (see below for nested schema)
                      The payload for an infrastructure application which defines the port, protocol, and target attributes. Only applicable to Infrastructure Applications, in which case this field is required.
                    items:
                      properties:
                        port:
                          description: |-
                            (Number) The port that the targets use for the chosen communication protocol. A port cannot be assigned to multiple protocols.
                            The port that the targets use for the chosen communication protocol. A port cannot be assigned to multiple protocols.
                          type: number
                        protocol:
                          description: |-
                            (String) The communication protocol your application secures.
                            The communication protocol your application secures.
                          type: string
                        targetAttributes:
                          description: |-
                            (Block List, Min: 1) Contains a map of target attribute keys to target attribute values. (see below for nested schema)
                            Contains a map of target attribute keys to target attribute values.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) Friendly name of the Access Application.
                                  The key of the attribute.
                                type: string
                              values:
                                description: |-
                                  (List of String) The values of the attribute.
                                  The values of the attribute.
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  type:
                    description: |-
                      (String) The application type. Available values: app_launcher, bookmark, biso, dash_sso, saas, self_hosted, ssh, vnc, warp, infrastructure. Defaults to self_hosted.
                      The application type. Available values: `app_launcher`, `bookmark`, `biso`, `dash_sso`, `saas`, `self_hosted`, `ssh`, `vnc`, `warp`, `infrastructure`. Defaults to `self_hosted`.
                    type: string
                  zoneId:
                    description: |-
                      (String) The zone identifier to target for the resource. Conflicts with account_id.
                      The zone identifier to target for the resource. Conflicts with `account_id`.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accountId:
                    description: |-
                      (String) The account identifier to target for the resource. Conflicts with zone_id.
                      The account identifier to target for the resource. Conflicts with `zone_id`.
                    type: string
                  allowAuthenticateViaWarp:
                    description: |-
                      (Boolean) When set to true, users can authenticate to this application using their WARP session. When set to false this application will always require direct IdP authentication. This setting always overrides the organization setting for WARP authentication.
                      When set to true, users can authenticate to this application using their WARP session. When set to false this application will always require direct IdP authentication. This setting always overrides the organization setting for WARP authentication.
                    type: boolean
                  allowedIdps:
                    description: |-
                      (Set of String) The identity providers selected for the application.
                      The identity providers selected for the application.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  appLauncherLogoUrl:
                    description: |-
                      (String) The logo URL of the app launcher.
                      The logo URL of the app launcher.
                    type: string
                  appLauncherVisible:
                    description: |-
                      (Boolean) Option to show/hide applications in App Launcher. Defaults to true.
                      Option to show/hide applications in App Launcher. Defaults to `true`.
                    type: boolean
                  autoRedirectToIdentity:
                    description: |-
                      (Boolean) Option to skip identity provider selection if only one is configured in allowed_idps. Defaults to false.
                      Option to skip identity provider selection if only one is configured in `allowed_idps`. Defaults to `false`.
                    type: boolean
                  bgColor:
                    description: |-
                      (String) The background color of the app launcher.
                      The background color of the app launcher.
                    type: string
                  corsHeaders:
                    description: |-
                      (Block List) CORS configuration for the Access Application. See below for reference structure. (see below for nested schema)
                      CORS configuration for the Access Application. See below for reference structure.
                    items:
                      properties:
                        allowAllHeaders:
                          description: |-
                            (Boolean) Value to determine whether all HTTP headers are exposed.
                            Value to determine whether all HTTP headers are exposed.
                          type: boolean
                        allowAllMethods:
                          description: |-
                            (Boolean) Value to determine whether all methods are exposed.
                            Value to determine whether all methods are exposed.
                          type: boolean
                        allowAllOrigins:
                          description: |-
                            (Boolean) Value to determine whether all origins are permitted to make CORS requests.
                            Value to determine whether all origins are permitted to make CORS requests.
                          type: boolean
                        allowCredentials:
                          description: |-
                            (Boolean) Value to determine if credentials (cookies, authorization headers, or TLS client certificates) are included with requests.
                            Value to determine if credentials (cookies, authorization headers, or TLS client certificates) are included with requests.
                          type: boolean
                        allowedHeaders:
                          description: |-
                            (Set of String) List of HTTP headers to expose via CORS.
                            List of HTTP headers to expose via CORS.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        allowedMethods:
                          description: |-
                            (Set of String) List of methods to expose via CORS.
                            List of methods to expose via CORS.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        allowedOrigins:
                          description: |-
                            (Set of String) List of origins permitted to make CORS requests.
                            List of origins permitted to make CORS requests.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        maxAge:
                          description: |-
                            (Number) The maximum time a preflight request will be cached.
                            The maximum time a preflight request will be cached.
                          type: number
                      type: object
                    type: array
                  customDenyMessage:
                    description: |-
                      (String) Option that returns a custom error message when a user is denied access to the application.
                      Option that returns a custom error message when a user is denied access to the application.
                    type: string
                  customDenyUrl:
                    description: |-
                      (String) Option that redirects to a custom URL when a user is denied access to the application via identity based rules.
                      Option that redirects to a custom URL when a user is denied access to the application via identity based rules.
                    type: string
                  customNonIdentityDenyUrl:
                    description: |-
                      (String) Option that redirects to a custom URL when a user is denied access to the application via non identity rules.
                      Option that redirects to a custom URL when a user is denied access to the application via non identity rules.
                    type: string
                  customPages:
                    description: |-
                      (Set of String) The custom pages selected for the application.
                      The custom pages selected for the application.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  destinations:
                    description: |-
                      (Block List) A destination secured by Access. Only present for self_hosted, vnc, and ssh applications. Always includes the value set as domain. Supersedes self_hosted_domains to allow for more flexibility in defining different types of destinations. Conflicts with self_hosted_domains. (see below for nested schema)
                      A destination secured by Access. Only present for self_hosted, vnc, and ssh applications. Always includes the value set as `domain`. Supersedes `self_hosted_domains` to allow for more flexibility in defining different types of destinations. Conflicts with `self_hosted_domains`.
                    items:
                      properties:
                        cidr:
                          description: |-
                            (String) The private CIDR of the destination. Only valid when type=private. IPs are computed as /32 cidr. Private destinations are an early access feature and gated behind a feature flag.
                            The private CIDR of the destination. Only valid when type=private. IPs are computed as /32 cidr. Private destinations are an early access feature and gated behind a feature flag.
                          type: string
                        hostname:
                          description: |-
                            (String) The private hostname of the destination. Only valid when type=private. Private hostnames currently match only Server Name Indications (SNI). Private destinations are an early access feature and gated behind a feature flag.
                            The private hostname of the destination. Only valid when type=private. Private hostnames currently match only Server Name Indications (SNI). Private destinations are an early access feature and gated behind a feature flag.
                          type: string
                        l4Protocol:
                          description: |-
                            (String) The l4 protocol that matches this destination. Only valid when type=private. Private destinations are an early access feature and gated behind a feature flag.
                            The l4 protocol that matches this destination. Only valid when type=private. Private destinations are an early access feature and gated behind a feature flag.
                          type: string
                        portRange:
                          description: |-
                            (String) The port range of the destination. Only valid when type=private. Single ports are supported. Private destinations are an early access feature and gated behind a feature flag.
                            The port range of the destination. Only valid when type=private. Single ports are supported. Private destinations are an early access feature and gated behind a feature flag.
                          type: string
                        type:
                          description: |-
                            (String) The application type. Available values: app_launcher, bookmark, biso, dash_sso, saas, self_hosted, ssh, vnc, warp, infrastructure. Defaults to self_hosted.
                            The destination type. Available values: `public`, `private`. Defaults to `public`.
                          type: string
                        uri:
                          description: |-
                            (String) The public URI of the destination. Can include a domain and path with wildcards. Only valid when type=public.
                            The public URI of the destination. Can include a domain and path with wildcards. Only valid when type=public.
                          type: string
                        vnetId:
                          description: |-
                            (String) The VNet ID of the destination. Only valid when type=private. Private destinations are an early access feature and gated behind a feature flag.
                            The VNet ID of the destination. Only valid when type=private. Private destinations are an early access feature and gated behind a feature flag.
                          type: string
                      type: object
                    type: array
                  domain:
                    description: |-
                      (String) The primary hostname and path that Access will secure. If the app is visible in the App Launcher dashboard, this is the domain that will be displayed.
                      The primary hostname and path that Access will secure. If the app is visible in the App Launcher dashboard, this is the domain that will be displayed.
                    type: string
                  domainType:
                    description: |-
                      (String) The type of the primary domain. Available values: public, private.
                      The type of the primary domain. Available values: `public`, `private`.
                    type: string
                  enableBindingCookie:
                    description: |-
                      (Boolean) Option to provide increased security against compromised authorization tokens and CSRF attacks by requiring an additional "binding" cookie on requests. Defaults to false.
                      Option to provide increased security against compromised authorization tokens and CSRF attacks by requiring an additional "binding" cookie on requests. Defaults to `false`.
                    type: boolean
                  footerLinks:
                    description: |-
                      (Block Set) The footer links of the app launcher. (see below for nested schema)
                      The footer links of the app launcher.
                    items:
                      properties:
                        name:
                          description: |-
                            (String) Friendly name of the Access Application.
                            The name of the footer link.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the footer link.
                            The URL of the footer link.
                          type: string
                      type: object
                    type: array
                  headerBgColor:
                    description: |-
                      (String) The background color of the header bar in the app launcher.
                      The background color of the header bar in the app launcher.
                    type: string
                  httpOnlyCookieAttribute:
                    description: |-
                      (Boolean) Option to add the HttpOnly cookie flag to access tokens.
                      Option to add the `HttpOnly` cookie flag to access tokens.
                    type: boolean
                  landingPageDesign:
                    description: |-
                      (Block List, Max: 1) The landing page design of the app launcher. (see below for nested schema)
                      The landing page design of the app launcher.
                    items:
                      properties:
                        buttonColor:
                          description: |-
                            (String) The button color of the landing page.
                            The button color of the landing page.
                          type: string
                        buttonTextColor:
                          description: |-
                            (String) The button text color of the landing page.
                            The button text color of the landing page.
                          type: string
                        imageUrl:
                          description: |-
                            (String) The URL of the image to be displayed in the landing page.
                            The URL of the image to be displayed in the landing page.
                          type: string
                        message:
                          description: |-
                            (String) The message of the landing page.
                            The message of the landing page.
                          type: string
                        title:
                          description: |-
                            (String) The title of the landing page.
                            The title of the landing page.
                          type: string
                      type: object
                    type: array
                  logoUrl:
                    description: |-
                      (String) Image URL for the logo shown in the app launcher dashboard.
                      Image URL for the logo shown in the app launcher dashboard.
                    type: string
                  name:
                    description: |-
                      (String) Friendly name of the Access Application.
                      Friendly name of the Access Application.
                    type: string
                  optionsPreflightBypass:
                    description: |-
                      (Boolean) Allows options preflight requests to bypass Access authentication and go directly to the origin. Cannot turn on if cors_headers is set. Defaults to false.
                      Allows options preflight requests to bypass Access authentication and go directly to the origin. Cannot turn on if cors_headers is set. Defaults to `false`.
                    type: boolean
                  policies:
                    description: |-
                      (List of String) The policies associated with the application, in ascending order of precedence. Warning: Do not use this field while you still have this application ID referenced as application_id in any cloudflare_access_policy resource, as it can result in an inconsistent state.
                      The policies associated with the application, in ascending order of precedence. Warning: Do not use this field while you still have this application ID referenced as `application_id` in any `cloudflare_access_policy` resource, as it can result in an inconsistent state.
                    items:
                      type: string
                    type: array
                  saasApp:
                    description: |-
                      (Block List, Max: 1) SaaS configuration for the Access Application. (see below for nested schema)
                      SaaS configuration for the Access Application.
                    items:
                      properties:
                        accessTokenLifetime:
                          description: |-
                            (String) The lifetime of the Access Token after creation. Valid units are m and h. Must be greater than or equal to 1m and less than or equal to 24h.
                            The lifetime of the Access Token after creation. Valid units are `m` and `h`. Must be greater than or equal to 1m and less than or equal to 24h.
                          type: string
                        allowPkceWithoutClientSecret:
                          description: |-
                            (Boolean) Allow PKCE flow without a client secret.
                            Allow PKCE flow without a client secret.
                          type: boolean
                        appLauncherUrl:
                          description: |-
                            (String) The URL where this applications tile redirects users.
                            The URL where this applications tile redirects users.
                          type: string
                        authType:
                          description: |-
                            (String) Modifying this attribute will force creation of a new resource.
                            **Modifying this attribute will force creation of a new resource.**
                          type: string
                        consumerServiceUrl:
                          description: |-
                            (String) The service provider's endpoint that is responsible for receiving and parsing a SAML assertion.
                            The service provider's endpoint that is responsible for receiving and parsing a SAML assertion.
                          type: string
                        customAttribute:
                          description: |-
                            (Block List) Custom attribute mapped from IDPs. (see below for nested schema)
                            Custom attribute mapped from IDPs.
                          items:
                            properties:
                              friendlyName:
                                description: |-
                                  (String) A friendly name for the attribute as provided to the SaaS app.
                                  A friendly name for the attribute as provided to the SaaS app.
                                type: string
                              name:
                                description: |-
                                  (String) Friendly name of the Access Application.
                                  The name of the attribute as provided to the SaaS app.
                                type: string
                              nameFormat:
                                description: |-
                                  (String) A globally unique name for an identity or service provider.
                                  A globally unique name for an identity or service provider.
                                type: string
                              required:
                                description: |-
                                  (Boolean) True if the attribute must be always present.
                                  True if the attribute must be always present.
                                type: boolean
                              source:
                                description: '(Block List, Min: 1, Max: 1) (see below
                                  for nested schema)'
                                items:
                                  properties:
                                    name:
                                      description: |-
                                        (String) Friendly name of the Access Application.
                                        The name of the attribute as provided by the IDP.
                                      type: string
                                    nameByIdp:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) A mapping from IdP ID to claim name.
                                        A mapping from IdP ID to claim name.
                                      type: object
                                      x-kubernetes-map-type: granular
                                  type: object
                                type: array
                            type: object
                          type: array
                        customClaim:
                          description: |-
                            (Block List) Custom claim mapped from IDPs. (see below for nested schema)
                            Custom claim mapped from IDPs.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) Friendly name of the Access Application.
                                  The name of the attribute as provided to the SaaS app.
                                type: string
                              required:
                                description: |-
                                  (Boolean) True if the attribute must be always present.
                                  True if the attribute must be always present.
                                type: boolean
                              scope:
                                description: |-
                                  (String) The scope of the claim.
                                  The scope of the claim.
                                type: string
                              source:
                                description: '(Block List, Min: 1, Max: 1) (see below
                                  for nested schema)'
                                items:
                                  properties:
                                    name:
                                      description: |-
                                        (String) Friendly name of the Access Application.
                                        The name of the attribute as provided by the IDP.
                                      type: string
                                    nameByIdp:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) A mapping from IdP ID to claim name.
                                        A mapping from IdP ID to claim name.
                                      type: object
                                      x-kubernetes-map-type: granular
                                  type: object
                                type: array
                            type: object
                          type: array
                        defaultRelayState:
                          description: |-
                            (String) The relay state used if not provided by the identity provider.
                            The relay state used if not provided by the identity provider.
                          type: string
                        grantTypes:
                          description: |-
                            (Set of String) The OIDC flows supported by this application.
                            The OIDC flows supported by this application.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        groupFilterRegex:
                          description: |-
                            (String) A regex to filter Cloudflare groups returned in ID token and userinfo endpoint.
                            A regex to filter Cloudflare groups returned in ID token and userinfo endpoint.
                          type: string
                        hybridAndImplicitOptions:
                          description: |-
                            (Block List, Max: 1) Hybrid and Implicit Flow options. (see below for nested schema)
                            Hybrid and Implicit Flow options.
                          items:
                            properties:
                              returnAccessTokenFromAuthorizationEndpoint:
                                description: |-
                                  (Boolean) If true, the authorization endpoint will return an access token.
                                  If true, the authorization endpoint will return an access token.
                                type: boolean
                              returnIdTokenFromAuthorizationEndpoint:
                                description: |-
                                  (Boolean) If true, the authorization endpoint will return an id token.
                                  If true, the authorization endpoint will return an id token.
                                type: boolean
                            type: object
                          type: array
                        nameIdFormat:
                          description: |-
                            (String) The format of the name identifier sent to the SaaS application.
                            The format of the name identifier sent to the SaaS application.
                          type: string
                        nameIdTransformJsonata:
                          description: |-
                            (String) A JSONata expression that transforms an application's user identities into a NameID value for its SAML assertion. This expression should evaluate to a singular string. The output of this expression can override the name_id_format setting.
                            A [JSONata](https://jsonata.org/) expression that transforms an application's user identities into a NameID value for its SAML assertion. This expression should evaluate to a singular string. The output of this expression can override the `name_id_format` setting.
                          type: string
                        redirectUris:
                          description: |-
                            (Set of String) The permitted URL's for Cloudflare to return Authorization codes and Access/ID tokens.
                            The permitted URL's for Cloudflare to return Authorization codes and Access/ID tokens.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        refreshTokenOptions:
                          description: |-
                            (Block List) Refresh token grant options. (see below for nested schema)
                            Refresh token grant options.
                          items:
                            properties:
                              lifetime:
                                description: |-
                                  (String) How long a refresh token will be valid for after creation. Valid units are m, h and d. Must be longer than 1m.
                                  How long a refresh token will be valid for after creation. Valid units are `m`, `h` and `d`. Must be longer than 1m.
                                type: string
                            type: object
                          type: array
                        samlAttributeTransformJsonata:
                          description: |-
                            (String) A JSONata expression that transforms an application's user identities into attribute assertions in the SAML response. The expression can transform id, email, name, and groups values. It can also transform fields listed in the saml_attributes or oidc_fields of the identity provider used to authenticate. The output of this expression must be a JSON object.
                            A [JSONata](https://jsonata.org/) expression that transforms an application's user identities into attribute assertions in the SAML response. The expression can transform id, email, name, and groups values. It can also transform fields listed in the saml_attributes or oidc_fields of the identity provider used to authenticate. The output of this expression must be a JSON object.
                          type: string
                        scopes:
                          description: |-
                            (Set of String) Define the user information shared with access.
                            Define the user information shared with access.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        spEntityId:
                          description: |-
                            (String) A globally unique name for an identity or service provider.
                            A globally unique name for an identity or service provider.
                          type: string
                      type: object
                    type: array
                  sameSiteCookieAttribute:
                    description: |-
                      site cookie setting for access tokens. Available values: none, lax, strict.
                      Defines the same-site cookie setting for access tokens. Available values: `none`, `lax`, `strict`.
                    type: string
                  scimConfig:
                    description: |-
                      (Block List, Max: 1) Configuration for provisioning to this application via SCIM. This is currently in closed beta. (see below for nested schema)
                      Configuration for provisioning to this application via SCIM. This is currently in closed beta.
                    items:
                      properties:
                        authentication:
                          description: |-
                            (Block List) Attributes for configuring HTTP Basic, OAuth Bearer token, or OAuth 2 authentication schemes for SCIM provisioning to an application. (see below for nested schema)
                            Attributes for configuring HTTP Basic, OAuth Bearer token, or OAuth 2 authentication schemes for SCIM provisioning to an application.
                          items:
                            properties:
                              authorizationUrl:
                                description: |-
                                  (String) URL used to generate the auth code used during token generation.
                                  URL used to generate the auth code used during token generation.
                                type: string
                              clientId:
                                description: |-
                                  (String) The application client id.
                                  Client ID used to authenticate when generating a token for authenticating with the remote SCIM service.
                                type: string
                              clientSecret:
                                description: |-
                                  (String, Sensitive) The application client secret, only returned on initial apply.
                                  Secret used to authenticate when generating a token for authenticating with the remove SCIM service.
                                type: string
                              password:
                                description: (String)
                                type: string
                              scheme:
                                description: |-
                                  (String) The authentication scheme to use when making SCIM requests to this application.
                                  The authentication scheme to use when making SCIM requests to this application.
                                type: string
                              scopes:
                                description: |-
                                  (Set of String) Define the user information shared with access.
                                  The authorization scopes to request when generating the token used to authenticate with the remove SCIM service.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              token:
                                description: |-
                                  (String) Token used to authenticate with the remote SCIM service.
                                  Token used to authenticate with the remote SCIM service.
                                type: string
                              tokenUrl:
                                description: |-
                                  (String) URL used to generate the token used to authenticate with the remote SCIM service.
                                  URL used to generate the token used to authenticate with the remote SCIM service.
                                type: string
                              user:
                                description: |-
                                  (String) User name used to authenticate with the remote SCIM service.
                                  User name used to authenticate with the remote SCIM service.
                                type: string
                            type: object
                          type: array
                        deactivateOnDelete:
                          description: |-
                            (Boolean) If false, propagates DELETE requests to the target application for SCIM resources. If true, sets 'active' to false on the SCIM resource. Note: Some targets do not support DELETE operations.
                            If false, propagates DELETE requests to the target application for SCIM resources. If true, sets 'active' to false on the SCIM resource. Note: Some targets do not support DELETE operations.
                          type: boolean
                        enabled:
                          description: |-
                            (Boolean) Whether SCIM provisioning is turned on for this application.
                            Whether SCIM provisioning is turned on for this application.
                          type: boolean
                        idpUid:
                          description: |-
                            (String) The UIDs of the IdP to use as the source for SCIM resources to provision to this application.
                            The UIDs of the IdP to use as the source for SCIM resources to provision to this application.
                          type: string
                        mappings:
                          description: |-
                            (Block List) A list of mappings to apply to SCIM resources before provisioning them in this application. These can transform or filter the resources to be provisioned. (see below for nested schema)
                            A list of mappings to apply to SCIM resources before provisioning them in this application. These can transform or filter the resources to be provisioned.
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) Whether SCIM provisioning is turned on for this application.
                                  Whether or not this mapping is enabled.
                                type: boolean
                              filter:
                                description: |-
                                  (String) A SCIM filter expression that matches resources that should be provisioned to this application.
                                  A [SCIM filter expression](https://datatracker.ietf.org/doc/html/rfc7644#section-3.4.2.2) that matches resources that should be provisioned to this application.
                                type: string
                              operations:
                                description: |-
                                  (Block List, Max: 1) Whether or not this mapping applies to creates, updates, or deletes. (see below for nested schema)
                                  Whether or not this mapping applies to creates, updates, or deletes.
                                items:
                                  properties:
                                    create:
                                      description: |-
                                        (Boolean) Whether or not this mapping applies to create (POST) operations.
                                        Whether or not this mapping applies to create (POST) operations.
                                      type: boolean
                                    delete:
                                      description: |-
                                        (Boolean) Whether or not this mapping applies to DELETE operations.
                                        Whether or not this mapping applies to DELETE operations.
                                      type: boolean
                                    update:
                                      description: |-
                                        (Boolean) Whether or not this mapping applies to update (PATCH/PUT) operations.
                                        Whether or not this mapping applies to update (PATCH/PUT) operations.
                                      type: boolean
                                  type: object
                                type: array
                              schema:
                                description: |-
                                  (String) Which SCIM resource type this mapping applies to.
                                  Which SCIM resource type this mapping applies to.
                                type: string
                              strictness:
                                description: |-
                                  (String) How strictly to adhere to outbound resource schemas when provisioning to this mapping. "strict" will remove unknown values when provisioning, while "passthrough" will pass unknown values to the target.
                                  How strictly to adhere to outbound resource schemas when provisioning to this mapping. "strict" will remove unknown values when provisioning, while "passthrough" will pass unknown values to the target.
                                type: string
                              transformJsonata:
                                description: |-
                                  (String) A JSONata expression that transforms the resource before provisioning it in the application.
                                  A [JSONata](https://jsonata.org/) expression that transforms the resource before provisioning it in the application.
                                type: string
                            type: object
                          type: array
                        remoteUri:
                          description: |-
                            compatible API.
                            The base URI for the application's SCIM-compatible API.
                          type: string
                      type: object
                    type: array
                  selfHostedDomains:
                    description: |-
                      (Set of String, Deprecated) List of public domains secured by Access. Only present for self_hosted, vnc, and ssh applications. Always includes the value set as domain. Deprecated in favor of destinations and will be removed in the next major version. Conflicts with destinations.
                      List of public domains secured by Access. Only present for self_hosted, vnc, and ssh applications. Always includes the value set as `domain`. Deprecated in favor of `destinations` and will be removed in the next major version. Conflicts with `destinations`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  serviceAuth401Redirect:
                    description: |-
                      (Boolean) Option to return a 401 status code in service authentication rules on failed requests. Defaults to false.
                      Option to return a 401 status code in service authentication rules on failed requests. Defaults to `false`.
                    type: boolean
                  sessionDuration:
                    description: |-
                      authorise. Must be in the format 48h or 2h45m. Defaults to 24h.
                      How often a user will be forced to re-authorise. Must be in the format `48h` or `2h45m`. Defaults to `24h`.
                    type: string
                  skipAppLauncherLoginPage:
                    description: |-
                      (Boolean) Option to skip the App Launcher landing page. Defaults to false.
                      Option to skip the App Launcher landing page. Defaults to `false`.
                    type: boolean
                  skipInterstitial:
                    description: |-
                      (Boolean) Option to skip the authorization interstitial when using the CLI. Defaults to false.
                      Option to skip the authorization interstitial when using the CLI. Defaults to `false`.
                    type: boolean
                  tags:
                    description: |-
                      (Set of String) The itags associated with the application.
                      The itags associated with the application.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  targetCriteria:
                    description: |-
                      (Block List) The payload for an infrastructure application which defines the port, protocol, and target attributes. Only applicable to Infrastructure Applications, in which case this field is required. (see below for nested schema)
                      The payload for an infrastructure application which defines the port, protocol, and target attributes. Only applicable to Infrastructure Applications, in which case this field is required.
                    items:
                      properties:
                        port:
                          description: |-
                            (Number) The port that the targets use for the chosen communication protocol. A port cannot be assigned to multiple protocols.
                            The port that the targets use for the chosen communication protocol. A port cannot be assigned to multiple protocols.
                          type: number
                        protocol:
                          description: |-
                            (String) The communication protocol your application secures.
                            The communication protocol your application secures.
                          type: string
                        targetAttributes:
                          description: |-
                            (Block List, Min: 1) Contains a map of target attribute keys to target attribute values. (see below for nested schema)
                            Contains a map of target attribute keys to target attribute values.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) Friendly name of the Access Application.
                                  The key of the attribute.
                                type: string
                              values:
                                description: |-
                                  (List of String) The values of the attribute.
                                  The values of the attribute.
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  type:
                    description: |-
                      (String) The application type. Available values: app_launcher, bookmark, biso, dash_sso, saas, self_hosted, ssh, vnc, warp, infrastructure. Defaults to self_hosted.
                      The application type. Available values: `app_launcher`, `bookmark`, `biso`, `dash_sso`, `saas`, `self_hosted`, `ssh`, `vnc`, `warp`, `infrastructure`. Defaults to `self_hosted`.
                    type: string
                  zoneId:
                    description: |-
                      (String) The zone identifier to target for the resource. Conflicts with account_id.
                      The zone identifier to target for the resource. Conflicts with `account_id`.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ApplicationStatus defines the observed state of Application.
            properties:
              atProvider:
                properties:
                  accountId:
                    description: |-
                      (String) The account identifier to target for the resource. Conflicts with zone_id.
                      The account identifier to target for the resource. Conflicts with `zone_id`.
                    type: string
                  allowAuthenticateViaWarp:
                    description: |-
                      (Boolean) When set to true, users can authenticate to this application using their WARP session. When set to false this application will always require direct IdP authentication. This setting always overrides the organization setting for WARP authentication.
                      When set to true, users can authenticate to this application using their WARP session. When set to false this application will always require direct IdP authentication. This setting always overrides the organization setting for WARP authentication.
                    type: boolean
                  allowedIdps:
                    description: |-
                      (Set of String) The identity providers selected for the application.
                      The identity providers selected for the application.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  appLauncherLogoUrl:
                    description: |-
                      (String) The logo URL of the app launcher.
                      The logo URL of the app launcher.
                    type: string
                  appLauncherVisible:
                    description: |-
                      (Boolean) Option to show/hide applications in App Launcher. Defaults to true.
                      Option to show/hide applications in App Launcher. Defaults to `true`.
                    type: boolean
                  aud:
                    description: |-
                      (String) Application Audience (AUD) Tag of the application.
                      Application Audience (AUD) Tag of the application.
                    type: string
                  autoRedirectToIdentity:
                    description: |-
                      (Boolean) Option to skip identity provider selection if only one is configured in allowed_idps. Defaults to false.
                      Option to skip identity provider selection if only one is configured in `allowed_idps`. Defaults to `false`.
                    type: boolean
                  bgColor:
                    description: |-
                      (String) The background color of the app launcher.
                      The background color of the app launcher.
                    type: string
                  corsHeaders:
                    description: |-
                      (Block List) CORS configuration for the Access Application. See below for reference structure. (see below for nested schema)
                      CORS configuration for the Access Application. See below for reference structure.
                    items:
                      properties:
                        allowAllHeaders:
                          description: |-
                            (Boolean) Value to determine whether all HTTP headers are exposed.
                            Value to determine whether all HTTP headers are exposed.
                          type: boolean
                        allowAllMethods:
                          description: |-
                            (Boolean) Value to determine whether all methods are exposed.
                            Value to determine whether all methods are exposed.
                          type: boolean
                        allowAllOrigins:
                          description: |-
                            (Boolean) Value to determine whether all origins are permitted to make CORS requests.
                            Value to determine whether all origins are permitted to make CORS requests.
                          type: boolean
                        allowCredentials:
                          description: |-
                            (Boolean) Value to determine if credentials (cookies, authorization headers, or TLS client certificates) are included with requests.
                            Value to determine if credentials (cookies, authorization headers, or TLS client certificates) are included with requests.
                          type: boolean
                        allowedHeaders:
                          description: |-
                            (Set of String) List of HTTP headers to expose via CORS.
                            List of HTTP headers to expose via CORS.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        allowedMethods:
                          description: |-
                            (Set of String) List of methods to expose via CORS.
                            List of methods to expose via CORS.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        allowedOrigins:
                          description: |-
                            (Set of String) List of origins permitted to make CORS requests.
                            List of origins permitted to make CORS requests.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        maxAge:
                          description: |-
                            (Number) The maximum time a preflight request will be cached.
                            The maximum time a preflight request will be cached.
                          type: number
                      type: object
                    type: array
                  customDenyMessage:
                    description: |-
                      (String) Option that returns a custom error message when a user is denied access to the application.
                      Option that returns a custom error message when a user is denied access to the application.
                    type: string
                  customDenyUrl:
                    description: |-
                      (String) Option that redirects to a custom URL when a user is denied access to the application via identity based rules.
                      Option that redirects to a custom URL when a user is denied access to the application via identity based rules.
                    type: string
                  customNonIdentityDenyUrl:
                    description: |-
                      (String) Option that redirects to a custom URL when a user is denied access to the application via non identity rules.
                      Option that redirects to a custom URL when a user is denied access to the application via non identity rules.
                    type: string
                  customPages:
                    description: |-
                      (Set of String) The custom pages selected for the application.
                      The custom pages selected for the application.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  destinations:
                    description: |-
                      (Block List) A destination secured by Access. Only present for self_hosted, vnc, and ssh applications. Always includes the value set as domain. Supersedes self_hosted_domains to allow for more flexibility in defining different types of destinations. Conflicts with self_hosted_domains. (see below for nested schema)
                      A destination secured by Access. Only present for self_hosted, vnc, and ssh applications. Always includes the value set as `domain`. Supersedes `self_hosted_domains` to allow for more flexibility in defining different types of destinations. Conflicts with `self_hosted_domains`.
                    items:
                      properties:
                        cidr:
                          description: |-
                            (String) The private CIDR of the destination. Only valid when type=private. IPs are computed as /32 cidr. Private destinations are an early access feature and gated behind a feature flag.
                            The private CIDR of the destination. Only valid when type=private. IPs are computed as /32 cidr. Private destinations are an early access feature and gated behind a feature flag.
                          type: string
                        hostname:
                          description: |-
                            (String) The private hostname of the destination. Only valid when type=private. Private hostnames currently match only Server Name Indications (SNI). Private destinations are an early access feature and gated behind a feature flag.
                            The private hostname of the destination. Only valid when type=private. Private hostnames currently match only Server Name Indications (SNI). Private destinations are an early access feature and gated behind a feature flag.
                          type: string
                        l4Protocol:
                          description: |-
                            (String) The l4 protocol that matches this destination. Only valid when type=private. Private destinations are an early access feature and gated behind a feature flag.
                            The l4 protocol that matches this destination. Only valid when type=private. Private destinations are an early access feature and gated behind a feature flag.
                          type: string
                        portRange:
                          description: |-
                            (String) The port range of the destination. Only valid when type=private. Single ports are supported. Private destinations are an early access feature and gated behind a feature flag.
                            The port range of the destination. Only valid when type=private. Single ports are supported. Private destinations are an early access feature and gated behind a feature flag.
                          type: string
                        type:
                          description: |-
                            (String) The application type. Available values: app_launcher, bookmark, biso, dash_sso, saas, self_hosted, ssh, vnc, warp, infrastructure. Defaults to self_hosted.
                            The destination type. Available values: `public`, `private`. Defaults to `public`.
                          type: string
                        uri:
                          description: |-
                            (String) The public URI of the destination. Can include a domain and path with wildcards. Only valid when type=public.
                            The public URI of the destination. Can include a domain and path with wildcards. Only valid when type=public.
                          type: string
                        vnetId:
                          description: |-
                            (String) The VNet ID of the destination. Only valid when type=private. Private destinations are an early access feature and gated behind a feature flag.
                            The VNet ID of the destination. Only valid when type=private. Private destinations are an early access feature and gated behind a feature flag.
                          type: string
                      type: object
                    type: array
                  domain:
                    description: |-
                      (String) The primary hostname and path that Access will secure. If the app is visible in the App Launcher dashboard, this is the domain that will be displayed.
                      The primary hostname and path that Access will secure. If the app is visible in the App Launcher dashboard, this is the domain that will be displayed.
                    type: string
                  domainType:
                    description: |-
                      (String) The type of the primary domain. Available values: public, private.
                      The type of the primary domain. Available values: `public`, `private`.
                    type: string
                  enableBindingCookie:
                    description: |-
                      (Boolean) Option to provide increased security against compromised authorization tokens and CSRF attacks by requiring an additional "binding" cookie on requests. Defaults to false.
                      Option to provide increased security against compromised authorization tokens and CSRF attacks by requiring an additional "binding" cookie on requests. Defaults to `false`.
                    type: boolean
                  footerLinks:
                    description: |-
                      (Block Set) The footer links of the app launcher. (see below for nested schema)
                      The footer links of the app launcher.
                    items:
                      properties:
                        name:
                          description: |-
                            (String) Friendly name of the Access Application.
                            The name of the footer link.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the footer link.
                            The URL of the footer link.
                          type: string
                      type: object
                    type: array
                  headerBgColor:
                    description: |-
                      (String) The background color of the header bar in the app launcher.
                      The background color of the header bar in the app launcher.
                    type: string
                  httpOnlyCookieAttribute:
                    description: |-
                      (Boolean) Option to add the HttpOnly cookie flag to access tokens.
                      Option to add the `HttpOnly` cookie flag to access tokens.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  landingPageDesign:
                    description: |-
                      (Block List, Max: 1) The landing page design of the app launcher. (see below for nested schema)
                      The landing page design of the app launcher.
                    items:
                      properties:
                        buttonColor:
                          description: |-
                            (String) The button color of the landing page.
                            The button color of the landing page.
                          type: string
                        buttonTextColor:
                          description: |-
                            (String) The button text color of the landing page.
                            The button text color of the landing page.
                          type: string
                        imageUrl:
                          description: |-
                            (String) The URL of the image to be displayed in the landing page.
                            The URL of the image to be displayed in the landing page.
                          type: string
                        message:
                          description: |-
                            (String) The message of the landing page.
                            The message of the landing page.
                          type: string
                        title:
                          description: |-
                            (String) The title of the landing page.
                            The title of the landing page.
                          type: string
                      type: object
                    type: array
                  logoUrl:
                    description: |-
                      (String) Image URL for the logo shown in the app launcher dashboard.
                      Image URL for the logo shown in the app launcher dashboard.
                    type: string
                  name:
                    description: |-
                      (String) Friendly name of the Access Application.
                      Friendly name of the Access Application.
                    type: string
                  optionsPreflightBypass:
                    description: |-
                      (Boolean) Allows options preflight requests to bypass Access authentication and go directly to the origin. Cannot turn on if cors_headers is set. Defaults to false.
                      Allows options preflight requests to bypass Access authentication and go directly to the origin. Cannot turn on if cors_headers is set. Defaults to `false`.
                    type: boolean
                  policies:
                    description: |-
                      (List of String) The policies associated with the application, in ascending order of precedence. Warning: Do not use this field while you still have this application ID referenced as application_id in any cloudflare_access_policy resource, as it can result in an inconsistent state.
                      The policies associated with the application, in ascending order of precedence. Warning: Do not use this field while you still have this application ID referenced as `application_id` in any `cloudflare_access_policy` resource, as it can result in an inconsistent state.
                    items:
                      type: string
                    type: array
                  saasApp:
                    description: |-
                      (Block List, Max: 1) SaaS configuration for the Access Application. (see below for nested schema)
                      SaaS configuration for the Access Application.
                    items:
                      properties:
                        accessTokenLifetime:
                          description: |-
                            (String) The lifetime of the Access Token after creation. Valid units are m and h. Must be greater than or equal to 1m and less than or equal to 24h.
                            The lifetime of the Access Token after creation. Valid units are `m` and `h`. Must be greater than or equal to 1m and less than or equal to 24h.
                          type: string
                        allowPkceWithoutClientSecret:
                          description: |-
                            (Boolean) Allow PKCE flow without a client secret.
                            Allow PKCE flow without a client secret.
                          type: boolean
                        appLauncherUrl:
                          description: |-
                            (String) The URL where this applications tile redirects users.
                            The URL where this applications tile redirects users.
                          type: string
                        authType:
                          description: |-
                            (String) Modifying this attribute will force creation of a new resource.
                            **Modifying this attribute will force creation of a new resource.**
                          type: string
                        clientId:
                          description: |-
                            (String) The application client id.
                            The application client id.
                          type: string
                        consumerServiceUrl:
                          description: |-
                            (String) The service provider's endpoint that is responsible for receiving and parsing a SAML assertion.
                            The service provider's endpoint that is responsible for receiving and parsing a SAML assertion.
                          type: string
                        customAttribute:
                          description: |-
                            (Block List) Custom attribute mapped from IDPs. (see below for nested schema)
                            Custom attribute mapped from IDPs.
                          items:
                            properties:
                              friendlyName:
                                description: |-
                                  (String) A friendly name for the attribute as provided to the SaaS app.
                                  A friendly name for the attribute as provided to the SaaS app.
                                type: string
                              name:
                                description: |-
                                  (String) Friendly name of the Access Application.
                                  The name of the attribute as provided to the SaaS app.
                                type: string
                              nameFormat:
                                description: |-
                                  (String) A globally unique name for an identity or service provider.
                                  A globally unique name for an identity or service provider.
                                type: string
                              required:
                                description: |-
                                  (Boolean) True if the attribute must be always present.
                                  True if the attribute must be always present.
                                type: boolean
                              source:
                                description: '(Block List, Min: 1, Max: 1) (see below
                                  for nested schema)'
                                items:
                                  properties:
                                    name:
                                      description: |-
                                        (String) Friendly name of the Access Application.
                                        The name of the attribute as provided by the IDP.
                                      type: string
                                    nameByIdp:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) A mapping from IdP ID to claim name.
                                        A mapping from IdP ID to claim name.
                                      type: object
                                      x-kubernetes-map-type: granular
                                  type: object
                                type: array
                            type: object
                          type: array
                        customClaim:
                          description: |-
                            (Block List) Custom claim mapped from IDPs. (see below for nested schema)
                            Custom claim mapped from IDPs.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) Friendly name of the Access Application.
                                  The name of the attribute as provided to the SaaS app.
                                type: string
                              required:
                                description: |-
                                  (Boolean) True if the attribute must be always present.
                                  True if the attribute must be always present.
                                type: boolean
                              scope:
                                description: |-
                                  (String) The scope of the claim.
                                  The scope of the claim.
                                type: string
                              source:
                                description: '(Block List, Min: 1, Max: 1) (see below
                                  for nested schema)'
                                items:
                                  properties:
                                    name:
                                      description: |-
                                        (String) Friendly name of the Access Application.
                                        The name of the attribute as provided by the IDP.
                                      type: string
                                    nameByIdp:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) A mapping from IdP ID to claim name.
                                        A mapping from IdP ID to claim name.
                                      type: object
                                      x-kubernetes-map-type: granular
                                  type: object
                                type: array
                            type: object
                          type: array
                        defaultRelayState:
                          description: |-
                            (String) The relay state used if not provided by the identity provider.
                            The relay state used if not provided by the identity provider.
                          type: string
                        grantTypes:
                          description: |-
                            (Set of String) The OIDC flows supported by this application.
                            The OIDC flows supported by this application.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        groupFilterRegex:
                          description: |-
                            (String) A regex to filter Cloudflare groups returned in ID token and userinfo endpoint.
                            A regex to filter Cloudflare groups returned in ID token and userinfo endpoint.
                          type: string
                        hybridAndImplicitOptions:
                          description: |-
                            (Block List, Max: 1) Hybrid and Implicit Flow options. (see below for nested schema)
                            Hybrid and Implicit Flow options.
                          items:
                            properties:
                              returnAccessTokenFromAuthorizationEndpoint:
                                description: |-
                                  (Boolean) If true, the authorization endpoint will return an access token.
                                  If true, the authorization endpoint will return an access token.
                                type: boolean
                              returnIdTokenFromAuthorizationEndpoint:
                                description: |-
                                  (Boolean) If true, the authorization endpoint will return an id token.
                                  If true, the authorization endpoint will return an id token.
                                type: boolean
                            type: object
                          type: array
                        idpEntityId:
                          description: |-
                            (String) The unique identifier for the SaaS application.
                            The unique identifier for the SaaS application.
                          type: string
                        nameIdFormat:
                          description: |-
                            (String) The format of the name identifier sent to the SaaS application.
                            The format of the name identifier sent to the SaaS application.
                          type: string
                        nameIdTransformJsonata:
                          description: |-
                            (String) A JSONata expression that transforms an application's user identities into a NameID value for its SAML assertion. This expression should evaluate to a singular string. The output of this expression can override the name_id_format setting.
                            A [JSONata](https://jsonata.org/) expression that transforms an application's user identities into a NameID value for its SAML assertion. This expression should evaluate to a singular string. The output of this expression can override the `name_id_format` setting.
                          type: string
                        publicKey:
                          description: |-
                            (String) The public certificate that will be used to verify identities.
                            The public certificate that will be used to verify identities.
                          type: string
                        redirectUris:
                          description: |-
                            (Set of String) The permitted URL's for Cloudflare to return Authorization codes and Access/ID tokens.
                            The permitted URL's for Cloudflare to return Authorization codes and Access/ID tokens.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        refreshTokenOptions:
                          description: |-
                            (Block List) Refresh token grant options. (see below for nested schema)
                            Refresh token grant options.
                          items:
                            properties:
                              lifetime:
                                description: |-
                                  (String) How long a refresh token will be valid for after creation. Valid units are m, h and d. Must be longer than 1m.
                                  How long a refresh token will be valid for after creation. Valid units are `m`, `h` and `d`. Must be longer than 1m.
                                type: string
                            type: object
                          type: array
                        samlAttributeTransformJsonata:
                          description: |-
                            (String) A JSONata expression that transforms an application's user identities into attribute assertions in the SAML response. The expression can transform id, email, name, and groups values. It can also transform fields listed in the saml_attributes or oidc_fields of the identity provider used to authenticate. The output of this expression must be a JSON object.
                            A [JSONata](https://jsonata.org/) expression that transforms an application's user identities into attribute assertions in the SAML response. The expression can transform id, email, name, and groups values. It can also transform fields listed in the saml_attributes or oidc_fields of the identity provider used to authenticate. The output of this expression must be a JSON object.
                          type: string
                        scopes:
                          description: |-
                            (Set of String) Define the user information shared with access.
                            Define the user information shared with access.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        spEntityId:
                          description: |-
                            (String) A globally unique name for an identity or service provider.
                            A globally unique name for an identity or service provider.
                          type: string
                        ssoEndpoint:
                          description: |-
                            (String) The endpoint where the SaaS application will send login requests.
                            The endpoint where the SaaS application will send login requests.
                          type: string
                      type: object
                    type: array
                  sameSiteCookieAttribute:
                    description: |-
                      site cookie setting for access tokens. Available values: none, lax, strict.
                      Defines the same-site cookie setting for access tokens. Available values: `none`, `lax`, `strict`.
                    type: string
                  scimConfig:
                    description: |-
                      (Block List, Max: 1) Configuration for provisioning to this application via SCIM. This is currently in closed beta. (see below for nested schema)
                      Configuration for provisioning to this application via SCIM. This is currently in closed beta.
                    items:
                      properties:
                        authentication:
                          description: |-
                            (Block List) Attributes for configuring HTTP Basic, OAuth Bearer token, or OAuth 2 authentication schemes for SCIM provisioning to an application. (see below for nested schema)
                            Attributes for configuring HTTP Basic, OAuth Bearer token, or OAuth 2 authentication schemes for SCIM provisioning to an application.
                          items:
                            properties:
                              authorizationUrl:
                                description: |-
                                  (String) URL used to generate the auth code used during token generation.
                                  URL used to generate the auth code used during token generation.
                                type: string
                              clientId:
                                description: |-
                                  (String) The application client id.
                                  Client ID used to authenticate when generating a token for authenticating with the remote SCIM service.
                                type: string
                              clientSecret:
                                description: |-
                                  (String, Sensitive) The application client secret, only returned on initial apply.
                                  Secret used to authenticate when generating a token for authenticating with the remove SCIM service.
                                type: string
                              password:
                                description: (String)
                                type: string
                              scheme:
                                description: |-
                                  (String) The authentication scheme to use when making SCIM requests to this application.
                                  The authentication scheme to use when making SCIM requests to this application.
                                type: string
                              scopes:
                                description: |-
                                  (Set of String) Define the user information shared with access.
                                  The authorization scopes to request when generating the token used to authenticate with the remove SCIM service.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              token:
                                description: |-
                                  (String) Token used to authenticate with the remote SCIM service.
                                  Token used to authenticate with the remote SCIM service.
                                type: string
                              tokenUrl:
                                description: |-
                                  (String) URL used to generate the token used to authenticate with the remote SCIM service.
                                  URL used to generate the token used to authenticate with the remote SCIM service.
                                type: string
                              user:
                                description: |-
                                  (String) User name used to authenticate with the remote SCIM service.
                                  User name used to authenticate with the remote SCIM service.
                                type: string
                            type: object
                          type: array
                        deactivateOnDelete:
                          description: |-
                            (Boolean) If false, propagates DELETE requests to the target application for SCIM resources. If true, sets 'active' to false on the SCIM resource. Note: Some targets do not support DELETE operations.
                            If false, propagates DELETE requests to the target application for SCIM resources. If true, sets 'active' to false on the SCIM resource. Note: Some targets do not support DELETE operations.
                          type: boolean
                        enabled:
                          description: |-
                            (Boolean) Whether SCIM provisioning is turned on for this application.
                            Whether SCIM provisioning is turned on for this application.
                          type: boolean
                        idpUid:
                          description: |-
                            (String) The UIDs of the IdP to use as the source for SCIM resources to provision to this application.
                            The UIDs of the IdP to use as the source for SCIM resources to provision to this application.
                          type: string
                        mappings:
                          description: |-
                            (Block List) A list of mappings to apply to SCIM resources before provisioning them in this application. These can transform or filter the resources to be provisioned. (see below for nested schema)
                            A list of mappings to apply to SCIM resources before provisioning them in this application. These can transform or filter the resources to be provisioned.
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) Whether SCIM provisioning is turned on for this application.
                                  Whether or not this mapping is enabled.
                                type: boolean
                              filter:
                                description: |-
                                  (String) A SCIM filter expression that matches resources that should be provisioned to this application.
                                  A [SCIM filter expression](https://datatracker.ietf.org/doc/html/rfc7644#section-3.4.2.2) that matches resources that should be provisioned to this application.
                                type: string
                              operations:
                                description: |-
                                  (Block List, Max: 1) Whether or not this mapping applies to creates, updates, or deletes. (see below for nested schema)
                                  Whether or not this mapping applies to creates, updates, or deletes.
                                items:
                                  properties:
                                    create:
                                      description: |-
                                        (Boolean) Whether or not this mapping applies to create (POST) operations.
                                        Whether or not this mapping applies to create (POST) operations.
                                      type: boolean
                                    delete:
                                      description: |-
                                        (Boolean) Whether or not this mapping applies to DELETE operations.
                                        Whether or not this mapping applies to DELETE operations.
                                      type: boolean
                                    update:
                                      description: |-
                                        (Boolean) Whether or not this mapping applies to update (PATCH/PUT) operations.
                                        Whether or not this mapping applies to update (PATCH/PUT) operations.
                                      type: boolean
                                  type: object
                                type: array
                              schema:
                                description: |-
                                  (String) Which SCIM resource type this mapping applies to.
                                  Which SCIM resource type this mapping applies to.
                                type: string
                              strictness:
                                description: |-
                                  (String) How strictly to adhere to outbound resource schemas when provisioning to this mapping. "strict" will remove unknown values when provisioning, while "passthrough" will pass unknown values to the target.
                                  How strictly to adhere to outbound resource schemas when provisioning to this mapping. "strict" will remove unknown values when provisioning, while "passthrough" will pass unknown values to the target.
                                type: string
                              transformJsonata:
                                description: |-
                                  (String) A JSONata expression that transforms the resource before provisioning it in the application.
                                  A [JSONata](https://jsonata.org/) expression that transforms the resource before provisioning it in the application.
                                type: string
                            type: object
                          type: array
                        remoteUri:
                          description: |-
                            compatible API.
                            The base URI for the application's SCIM-compatible API.
                          type: string
                      type: object
                    type: array
                  selfHostedDomains:
                    description: |-
                      (Set of String, Deprecated) List of public domains secured by Access. Only present for self_hosted, vnc, and ssh applications. Always includes the value set as domain. Deprecated in favor of destinations and will be removed in the next major version. Conflicts with destinations.
                      List of public domains secured by Access. Only present for self_hosted, vnc, and ssh applications. Always includes the value set as `domain`. Deprecated in favor of `destinations` and will be removed in the next major version. Conflicts with `destinations`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  serviceAuth401Redirect:
                    description: |-
                      (Boolean) Option to return a 401 status code in service authentication rules on failed requests. Defaults to false.
                      Option to return a 401 status code in service authentication rules on failed requests. Defaults to `false`.
                    type: boolean
                  sessionDuration:
                    description: |-
                      authorise. Must be in the format 48h or 2h45m. Defaults to 24h.
                      How often a user will be forced to re-authorise. Must be in the format `48h` or `2h45m`. Defaults to `24h`.
                    type: string
                  skipAppLauncherLoginPage:
                    description: |-
                      (Boolean) Option to skip the App Launcher landing page. Defaults to false.
                      Option to skip the App Launcher landing page. Defaults to `false`.
                    type: boolean
                  skipInterstitial:
                    description: |-
                      (Boolean) Option to skip the authorization interstitial when using the CLI. Defaults to false.
                      Option to skip the authorization interstitial when using the CLI. Defaults to `false`.
                    type: boolean
                  tags:
                    description: |-
                      (Set of String) The itags associated with the application.
                      The itags associated with the application.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  targetCriteria:
                    description: |-
                      (Block List) The payload for an infrastructure application which defines the port, protocol, and target attributes. Only applicable to Infrastructure Applications, in which case this field is required. (see below for nested schema)
                      The payload for an infrastructure application which defines the port, protocol, and target attributes. Only applicable to Infrastructure Applications, in which case this field is required.
                    items:
                      properties:
                        port:
                          description: |-
                            (Number) The port that the targets use for the chosen communication protocol. A port cannot be assigned to multiple protocols.
                            The port that the targets use for the chosen communication protocol. A port cannot be assigned to multiple protocols.
                          type: number
                        protocol:
                          description: |-
                            (String) The communication protocol your application secures.
                            The communication protocol your application secures.
                          type: string
                        targetAttributes:
                          description: |-
                            (Block List, Min: 1) Contains a map of target attribute keys to target attribute values. (see below for nested schema)
                            Contains a map of target attribute keys to target attribute values.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) Friendly name of the Access Application.
                                  The key of the attribute.
                                type: string
                              values:
                                description: |-
                                  (List of String) The values of the attribute.
                                  The values of the attribute.
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  type:
                    description: |-
                      (String) The application type. Available values: app_launcher, bookmark, biso, dash_sso, saas, self_hosted, ssh, vnc, warp, infrastructure. Defaults to self_hosted.
                      The application type. Available values: `app_launcher`, `bookmark`, `biso`, `dash_sso`, `saas`, `self_hosted`, `ssh`, `vnc`, `warp`, `infrastructure`. Defaults to `self_hosted`.
                    type: string
                  zoneId:
                    description: |-
                      (String) The zone identifier to target for the resource. Conflicts with account_id.
                      The zone identifier to target for the resource. Conflicts with `account_id`.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
